# hyper-compress-linux-x64

Prebuilt Hyper Compress engine binaries for Linux x64. This package is installed
automatically as an optional dependency of
[hyper-compress](https://www.npmjs.com/package/hyper-compress);
do not install it directly.
